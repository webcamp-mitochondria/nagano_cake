<h3>ジャンル編集</h3>
<%= form_with model: @genre do |f| %>
  <div class="field">
    <%= f.label :name, "ジャンル名" %>
    <%= f.text_field :name, placeholder: "ジャンル名" %>
    <%= f.submit "変更を保存", class: 'btn btn-success' %>
  </div>
<% end %>
